I would like to convert the script found in monitor.sh into a nodejs application that performs the same functionality to alter a bitaxe's settings via the bitaxe api.

The settings file such as monitor_settings.json give an example of parameters to enable quick startup of the script which is designed to loop indefinitely.